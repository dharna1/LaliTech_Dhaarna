name: CI/CD

on:
 push:
  branches: ["main"]

jobs:
 build:
  runs-on: ubuntu-latest

  steps:
   - name: Checkout code
     uses: actions/checkout@v3

   - name: Build Docker
     run: |
      echo "Files in repo:"
      ls -la

    - name: Debug files
      run: |
       echo  "Starting Docker Build..."
       docker build -t backend-app .
